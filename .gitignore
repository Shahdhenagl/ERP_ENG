*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# NOTE: public/build, public/sw.js and public/manifest.webmanifest ARE committed
# on purpose. Hostinger shared hosting has no Node, so the frontend is built
# locally and shipped in the repo — `git pull` on the server must land working
# assets. Run `npm run build` before every deploy commit.

# Screenshots produced by tests/Browser/uicheck.mjs
/storage/app/screenshots

# Leftover empty directory locked by a shell during scaffolding
/_scaffold
storage/framework/browser-tests.lock
